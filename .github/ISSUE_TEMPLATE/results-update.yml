name: Results update (organizer only)
description: Publish real match results (generated by the admin page)
title: "Results update"
labels: ["results-update"]
body:
  - type: markdown
    attributes:
      value: |
        🛠️ Organizer tool. Only the repo owner/collaborators can publish results —
        submissions from anyone else are ignored.
  - type: textarea
    id: payload
    attributes:
      label: Results JSON
      description: Contents of data/results.json, generated by admin.html
      render: json
    validations:
      required: true
